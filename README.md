## Useful functions for webpages creation:

- useForm - Handles the value of multiple inputs and puts it in and array
- results - Searches through a database to find the input value in the keys of the database
